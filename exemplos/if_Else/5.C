#include <stdio.h>

int main(){

    int x, y, menor;
        scanf("%d%d",&x, &y);
            if (x < y){
                menor = x;
            }
            else{
                menor = y;
            }
    printf("%d", menor);



return 0;

}